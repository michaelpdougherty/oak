extends layout

block layout-content
  div.View.WelcomeView
    h1(onclick="window.location='/'").Banner OAK
    div.Message.Center
      div.Title
        h3(id="header") Login
      if err
        div(id="err").Err #{err}
      form(method="post", action="/login", id="login-form")
        div.form-group
          input(placeholder="Username", name="username", type="text").form-control
        div.form-group
          input(placeholder="Password", name="password", type="password").form-control
        div.NavButtons
          button(type="submit", id="go").NavButton Go

  script(src="/login.js")
